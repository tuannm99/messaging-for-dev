// rabbitmq stream available in version 0.9.1 or higher
// It probably new so now I don't give it example,
// If I need a streaming platform => I probly use Kafka (with stream Rabbitmq got mostly everything similar to Kafka)
// we can checkout latest document - https://www.rabbitmq.com/docs/streams
